import { BackgrounderDirective } from './backgrounder.directive';

describe('BackgrounderDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgrounderDirective();
    expect(directive).toBeTruthy();
  });
});
